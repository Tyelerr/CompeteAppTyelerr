import { Text } from "react-native";
import ScreenScrollView from "../ScreenScrollView";

export default function ScreenSubmitAfter(){
  return <ScreenScrollView>
    <Text style={{color: 'white'}}>This will be the submit page after</Text>
  </ScreenScrollView>
}